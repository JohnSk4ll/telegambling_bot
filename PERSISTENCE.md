# 💾 Сохранение данных при удалении Docker контейнера

## ✅ Ваши данные защищены!

Все данные бота сохраняются на диск и **не теряются** при удалении контейнера.

### 📂 Что сохраняется:

1. **База данных** (`data/db.json`)
   - Все пользователи с их балансами, уровнями, инвентарем
   - Все кейсы и их настройки
   - Настройки наград за уровни
   - История и статистика

2. **Загруженные файлы** (`src/public/uploads/`)
   - Изображения предметов
   - Картинки кейсов
   - Все медиа-файлы

### 🔧 Как это работает:

В `docker-compose.yml` настроены **volumes** (тома):

```yaml
volumes:
  - ./data:/app/data              # База данных
  - ./uploads:/app/src/public/uploads  # Загруженные файлы
```

### 🧪 Проверка:

Можно смело удалять контейнер:
```bash
docker-compose down
docker rm telegambling_bot -f
```

И пересоздавать заново:
```bash
docker-compose up -d --build
```

**Все данные останутся!** ✨

### 📊 Файлы на диске:

- `data/db.json` - основная база данных (JSON формат)
- `src/public/uploads/` - папка с изображениями

Эти файлы находятся на вашем компьютере и не удаляются вместе с контейнером.

### ⚠️ Важно:

Для **полного удаления данных** нужно удалить сами файлы:
```bash
# Удалить базу данных
rm data/db.json

# Удалить загруженные файлы
rm -rf src/public/uploads/*
```

Но это нужно делать **только если вы действительно хотите начать заново!**
